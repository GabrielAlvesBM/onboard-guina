import Button from "../../../atomic/atm.button";

const LoginPage = () => {
  return (
    <main>
      <Button variant="primary" disabled={true}>
        Hello World!
      </Button>
    </main>
  );
};

export default LoginPage;

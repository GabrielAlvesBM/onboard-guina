query ListBoards($pageInput: PageInput!) {
  boards(pageInput: $pageInput) {
    count
    nodes {
      id
      name
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      limit
      offset
    }
  }
}